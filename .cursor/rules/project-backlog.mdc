---
description: Live project backlog is docs/todo.md — read on request, don't implement without an explicit go-ahead
alwaysApply: true
---

# Project backlog

- Open tasks and feature ideas live in [`docs/todo.md`](docs/todo.md).
- For questions about plans, backlog, "what's next", "check the todo" — read this file.
- Do not start implementing todo items without an explicit user request.
- Remove completed items from todo; history lives in `CHANGELOG.md`.
- todo covers only tool development (parser, index DB schema, MCP tools), not operations (rebuilding the portable, recreating databases, configuring MCP in the IDE).
