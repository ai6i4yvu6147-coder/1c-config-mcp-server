# Python
venv/
__pycache__/
.pytest_cache/
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info/
dist/
build/

# PyInstaller (ignore ad-hoc local specs; keep project build recipes)
*.spec
!1C-Config-Admin.spec
!1c-config-cli.spec
!1c-config-server.spec

# Portable сборка (внутри проекта — устаревший вариант)
Portable/

# Базы данных и служебные данные
databases/
*.db

# Runtime config (portable instance only)
projects.json

# Планы / заметки (служебные)
plans/
scratch/
.tasks/

# Одноразовые handoff-отчёты для других команд (не коммитить)
/HANDOFF-*.md

# Claude Code (локальные worktrees, не в репозиторий)
.claude/

# Исходники справки 1С (если подключаются отдельно)
# help_sources/

# VS Code
.vscode/
*.code-workspace

# Cursor — только локальные настройки
.cursor/settings.local.json

# Временные файлы
*.log
*.tmp
*.bak

# Windows
Thumbs.db
desktop.ini

# macOS
.DS_Store
